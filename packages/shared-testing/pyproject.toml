[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"

[project]
name = "shared-testing"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "pytest>=8.3.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/shared_testing"]

